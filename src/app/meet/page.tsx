import MeetBrowseContent from '@/components/pages/MeetBrowseContent';

export const metadata = {
  title: 'Meetups | ncoin',
  description:
    'Discover and join real-world meetups, workshops, and conferences with the nomad community.',
};

export default function MeetPage() {
  return <MeetBrowseContent />;
}
